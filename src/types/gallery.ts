export interface GalleryItem {
  id: string;
  title: string;
  imageDataUrl: string;
  gridSize: string;
  colorCount: number;
  beadCount: number;
  likes: number;
  createdAt: string;
  userLiked: boolean;
}
